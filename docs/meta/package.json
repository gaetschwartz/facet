{
  "name": "facet-hljs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Custom highlight.js bundle for facet rustdoc documentation",
  "scripts": {
    "dev": "pnpm run build && pnpm run serve",
    "build": "tsgo && vite build",
    "build:docs": "pnpm run build && node scripts/build-docs.js",
    "serve": "pnpm run build:docs && vite preview --outDir test-crate/target/doc",
    "typecheck": "tsgo"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@typescript/native-preview": "^7.0.0-dev.20250528",
    "vite": "^7.0.0"
  },
  "dependencies": {
    "highlight.js": "^11.11.1"
  }
}
